import QtQuick

// Cartão base do Centro de Controle (fundo translúcido + cantos arredondados)
Rectangle {
    color: Theme.card
    radius: Theme.radiusCard
    border.width: 1
    border.color: Theme.stroke
}
