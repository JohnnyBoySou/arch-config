#!/usr/bin/env bash
# Compatibilidade: o wallpaper agora e por workspace (wallpaper-workspace).
# Sem argumentos sorteia de novo so o workspace ativo; "--all" sorteia todos.
exec "$HOME/.local/bin/wallpaper-workspace" reroll "$@"
